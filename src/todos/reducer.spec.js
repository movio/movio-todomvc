import { expect } from 'chai';
import { reducers } from './reducer';

describe('First Test', () => {
  it('yep', () => {
    expect('foo').to.equal('foo');
  });
  it('truthiness', () => {
    expect(true).to.equal(true);
  });
});

